import * as React from 'react';
export interface SectionProps {
    title: string;
    actions?: JSX.Element;
    collapsed?: boolean;
}
declare class SectionFactory extends React.Component<SectionProps, {
    collapsed: boolean;
}> {
    constructor(props: SectionProps);
    render(): JSX.Element;
}
export declare const Section: import("styled-components").StyledComponent<typeof SectionFactory, any, SectionProps, never>;
export {};
