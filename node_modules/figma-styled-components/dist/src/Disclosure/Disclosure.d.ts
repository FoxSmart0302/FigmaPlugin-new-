import * as React from 'react';
export interface DisclosureProps {
    title: string;
    collapsed?: boolean;
}
declare class DisclosureFactory extends React.Component<DisclosureProps, {
    collapsed: boolean;
}> {
    constructor(props: DisclosureProps);
    render(): JSX.Element;
    private handleClick;
}
export declare const Disclosure: import("styled-components").StyledComponent<typeof DisclosureFactory, any, DisclosureProps, never>;
export {};
