import * as React from 'react';
import { IconNames } from './IconNames';
export interface IconProps {
    name: IconNames;
    color?: string;
    size?: number;
}
export declare const Icon: import("styled-components").StyledComponent<React.FunctionComponent<IconProps>, any, {}, never>;
